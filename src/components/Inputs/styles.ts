import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignSelf: 'center',
    paddingHorizontal: 40,
  },
  input: {
    marginBottom: -10,
    textAlign: 'center',
  }
});